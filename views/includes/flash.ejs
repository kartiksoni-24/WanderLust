<body>
    <% if(success && success.length) {%>
        <div class="mt-3 flash-suc-div col-md-4 offset-md-4" role="alert">
            <%= success %>
        </div>
        <% } %>

            <% if(error && error.length) {%>
                <div class="mt-3 flash-err-div col-md-4 offset-md-4" role="alert">
                    <%= error %>
                </div>
                <% } %>
</body>